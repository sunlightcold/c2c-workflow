# 需求与方案文档

这里存放公共平台能力、App 能力和前端联调方案。接口稳定后必须同步到 `doc/api/`，不能只保留在需求文档中。

## 当前条目

- [object-storage-center-design.md](object-storage-center-design.md)

## 规则

- 文件名统一使用英文 `kebab-case`。
- 一个文件只描述一个需求或方案。
- `main` 维护公共平台能力，`app` 维护 App 个性化能力。
- 涉及数据库结构时必须同步迁移计划和验证要求。
