#!/usr/bin/env sh
set -eu

repository_root="$(git rev-parse --show-toplevel)"
commit_message_file="$(cd "$(dirname "$1")" && pwd)/$(basename "$1")"

cd "$repository_root/projects/admin-web"
pnpm exec commitlint --edit "$commit_message_file"
